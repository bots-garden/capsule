# CapsCtl: a CLI for Capsule HTTP

## Build and install

```bash
task build-and-install-capsctl
```

## Start and call modules

```bash
task start-local-wasm-module
task call-hello-green
task call-hello-saved_green
```