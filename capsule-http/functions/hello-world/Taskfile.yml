version: '3'

tasks:

  build-wasm:
    cmds:
      - |
        tinygo build -o hello-world.wasm \
          -scheduler=none \
          --no-debug \
          -target wasi ./main.go 
        ls -lh *.wasm
